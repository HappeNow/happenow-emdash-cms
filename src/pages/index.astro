---
// Production: `/` is served by the main HappeNow site (Next.js on Zeabur).
// Cloudflare Workers Routes only forward /blog, /changelog, /_emdash to this Worker.
// This redirect is a safety net for local dev or misrouted requests.
return Astro.redirect("/blog", 302);
---
